query($query:String!) {
  search(query: $query, type: ISSUE, first: 100) {
    nodes {
      __typename
      ... on PullRequest {
        url
        title
        state
        repository {
          nameWithOwner
        }
        number
      }
    }
  }
}